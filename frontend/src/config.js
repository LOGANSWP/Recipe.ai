// You may use localhost for local testing.
// For deployment, change it to the server IP address.
const BASE_URL = "http://localhost:3000/api";

const RETRY_TIME = 3 * 1000;

export {
  BASE_URL,
  RETRY_TIME,
};
